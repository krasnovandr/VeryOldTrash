<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width" />
    <link  href="~/Content/Site.css"/ type="text/css" rel="stylesheet">
    <title>@ViewBag.Title</title>
    @Styles.Render("~/Content/css")
    @Scripts.Render("~/bundles/modernizr")

    @RenderSection("sTylesInHead",required:false)
</head>
<body>
        @Html.Partial("_LoginPartial")
     <nav>
          @if (Request.IsAuthenticated)
          {
                  <h2 id="Mylink">@Html.ActionLink("Show Your Devices Table", "Index", "Home")</h2>
                  <h2>@Html.ActionLink("Show empty Devicse Table", "EmptyDevices", "Home")</h2>
                  <h2>@Html.ActionLink("Send Email", "Send", "Home")</h2>    
                 
          }
        <ul class="menu">
        
 @*           <li>@Html.ActionLink("Главная", "Index", "Sample")</li>
            <li>@Html.ActionLink("Админ", "Index", "Admin")</li>*@

        </ul>
    </nav>
    @RenderBody()

    @*@Scripts.Render("~/bundles/jquery")*@
    @RenderSection("scripts", required: false)
</body>
</html>
